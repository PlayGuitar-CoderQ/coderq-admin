import React, { FC } from 'react';
import { observer } from 'mobx-react-lite';
import './index.less';

const Structure: FC = observer(() => {
  return <div>CoderQAdmin 架构图, 准备下一版引入ant x6数据架构</div>;
});

export default Structure;
